@page
@model Destination_Lajet.Pages.Organizer.OrganizeEventModel
@{
    ViewData["Title"] = "OrganizeEvents";
}

<h1>OrganizerEvents</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Place)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].SpotsAvailable)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Event)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Place)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SpotsAvailable)
            </td>
            <td>
                │ <a asp-page="/Organizer/Changes/EditEvent" asp-route-id="@item.EventId">Edit</a> │
                 <a asp-page="/Organizer/Changes/DetailsEvent" asp-route-id="@item.EventId">Details</a> │
                 <a asp-page="/Organizer/Changes/DeleteEvent" asp-route-id="@item.EventId">Delete</a> │
            </td>
        </tr>
        }
    </tbody>
</table>
